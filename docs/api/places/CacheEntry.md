# CacheEntry

a cached place entry, containing a places struct and its last updated timestamp. this is used internally by [`Cache`] to store place data.

## fields

- places: `Places`
- last_updated: `u64`
