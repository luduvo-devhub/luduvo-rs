# CacheEntry

a cached profile entry, containing a profile and its last updated timestamp. this is used internally by [`Cache`] to store profile data.

## fields

- users: `Query`
- last_updated: `u64`
